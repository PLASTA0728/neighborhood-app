import { Jost, Cousine } from 'next/font/google';

export const jost = Jost({ subsets: ['latin'], weight: ['200', '400'] });

export const cousine = Cousine({ subsets: ['latin'], weight: ['400', '700'] });